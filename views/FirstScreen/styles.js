import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },

  duck: {
    marginTop: 12,
    width: 200,
    height: 200,
    alignSelf: 'center',
  },
});

export default styles;
